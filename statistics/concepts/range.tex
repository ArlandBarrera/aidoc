The range is the difference between larget and smallest values. This a way to find the dispersion of the data set. However, if there is outliers in the data the range is of little use.

\begin{listequbox}
  {\text{Range} = x_\text{max} - x_\text{min}}{equrange}{Range}
\end{listequbox}
